<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Website</title>

    <link href="~/Content/webStyle.css" rel="stylesheet" />

    <!-- =====BOX ICONS===== -->
    <link href='https://cdn.jsdelivr.net/npm/boxicons@2.0.5/css/boxicons.min.css' rel='stylesheet'>
</head>
<body>


    @RenderBody()


    <!--===== SCROLL REVEAL =====-->
    <script src="https://unpkg.com/scrollreveal"></script>

    <!--===== MAIN JS =====-->
    <script src="~/Scripts/main.js"></script>
    @RenderSection("scripts", required: false)
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script src="~/Scripts/popper.min.js"></script>

    <script>
        $("#checkBoxForMessage").click(function () {
            if ($("#checkBoxForMessage").prop('checked')) {
                $("#SendMessage").prop('disabled', false);
            }
            else {
                $("#SendMessage").prop('disabled', true);
            }
        });

      @*  $("#SendMessage").click(function () {

            const contactData = JSON.stringify({
                FullName: $("#Contact_Form_name").val(),
                Email: $("#Contact_Form_email").val(),
                Message: $("#Contact_Form_message").val()
            });

            $.ajax({

                url: "@Url.Action("Create","Contact")",
                type: "POST",
                dataType: "json",
                data: contactData,
                success: function (response) {
                    alert("Thank you!. We will reach you soon.");
                },
                error: function (response) {
                    alert("Sorry! We cannot send your message.");
                }
            });
        });*@
    </script>
</body>
</html>
